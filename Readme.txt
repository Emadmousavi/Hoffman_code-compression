compress.py will compress the text
decompress.py decompress the binary file with .cmp format
main.py is the main program you should run and give a file path:
1- if the file is text then it will create .cmp compressed file
2- if file is .cmp then it will create the .txt decompressed file